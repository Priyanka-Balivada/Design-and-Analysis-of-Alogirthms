#include<stdio.h>

int[] quickSort(int arr[],int n)
{
	int pivot=partition;
	quickSort(int )
}

void main()
{
	int x=0,n;
	printf("Enter the number of elements : ");
	scanf("%d",&n);	
	int arr[n];
	int i=0;
	
	printf("Enter array elements : \n");
	for(i=0;i<n;i++)
		scanf("%d",&arr[i]);
		
	arr=quickSort(arr,n);
}
